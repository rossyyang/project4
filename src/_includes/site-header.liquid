<header class="site-header">
  {%- if page.url == "/"  -%}
  <h1 class="site-name">{{ siteData.name }}</h1>
  {%- else -%}
  <span class="site-name">{{ siteData.name }}</span>
  {%- endif -%}
</header>
